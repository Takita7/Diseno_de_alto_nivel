if(BUILD_TESTS)
    if(GTest_FOUND)
        add_subdirectory(llvm)
        add_subdirectory(ptx)
    else()
        message(WARNING "GTest not found. Skipping compiler tests.")
    endif()
endif()

message(STATUS "Compiler tests configured")
